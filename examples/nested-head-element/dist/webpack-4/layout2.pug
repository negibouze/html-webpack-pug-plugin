doctype html
html(lang="en")
    head
        style(type="text/css").
            body {
                font-size: 13px;
            }
        script(type="text/javascript").
            var MY_VAR = 'foobar';
        title Bodyweight training
        link(href="styles.css" rel="stylesheet")
    body
        img(src="logo.png")
        h1 nested head element
        script(src="bundle.js")